namespace CoinInsertionManagerPractice
{
    public class CoinValidator
    {
        public bool coinIsValid(InsertedCoin coinToValidate) 
        {
            // for (ValidCoin coin in ValidCoin.values()) {
            //     if ((coin.diameterInMillimeters() == coinToValidate.diameterInMillimeters())
            //         && (coin.weightInGrams() == coinToValidate.weightInGrams())) 
            //     {
            //         return true;
            //     }
            // }
            return false;
        }
    }
}